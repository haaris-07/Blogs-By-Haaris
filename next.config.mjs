/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allows images from GitHub avatars and assets
  images: {
    domains: ["avatars.githubusercontent.com"],
  },
};

export default nextConfig;
